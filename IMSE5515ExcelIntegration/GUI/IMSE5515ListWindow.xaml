<Window x:Class="IMSE5515ExcelIntegration.IMSE5515ListWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="300" Width="300" Title="IMSE5515 Loader">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FF4A6889" Offset="1" />
            <GradientStop Color="#FFE5E5E5" Offset="0.624" />
            <GradientStop Color="#FFD6DFE7" />
        </LinearGradientBrush>
    </Window.Background>
    <Grid Margin="0,0,2,0">
        <Label Content="DMC List" HorizontalAlignment="Left" Height="26" Margin="10,10,0,0" VerticalAlignment="Top" Width="72"/>
        <Label Content="(one DMC per line)" HorizontalAlignment="Left" Height="26" Margin="10,31,0,0" VerticalAlignment="Top" Width="113"/>
        <TextBox Margin="126,10,19,42" Text="{Binding DMCList}" TextWrapping="Wrap" AcceptsReturn="True"/>

        <Button Content="Export to Excel" HorizontalAlignment="Right" Margin="0,0,19,13" 
                Width="90" Command="{Binding TakeDataCommand}" Height="20" VerticalAlignment="Bottom"/>
    </Grid>
</Window>
